import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('NetflixClone', () => App);
AppRegistry.runApplication('NetflixClone', {
  rootTag: document.getElementById('root')
}); 